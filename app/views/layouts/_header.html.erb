<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="#"><%= t("un.title")%></a>
    </div>
        <div class='btn-toolbar pull-right'>
          <% if current_user %>
            <button class="btn btn-default RbtnMargin " type="button"><span class="glyphicon glyphicon-user"></span>
              <%= link_to "(#{ t("un.exit") + current_user.email})", signout_path, id: "sign_out" %>
            </button>
            <button class="btn btn-default RbtnMargin " type="button"><span class="	glyphicon glyphicon-envelope"></span>
               <%= link_to "#{ t("un.refresh_emails") }", incoming_messages_refresh_emails_path, method: :post %>
            </button>
    		  <% else %>
    	      <button class="btn btn-default RbtnMargin " type="button"><span class="glyphicon glyphicon-globe"></span>
    	        <%= link_to "#{ t("un.login") }", user_omniauth_authorize_path(:google_oauth2) %>
    	      </button>
    		  <% end %>
            <button class="btn btn-default dropdown-toggle RbtnMargin" type="button" data-toggle="dropdown"><%= t("un.language")%>
            <span class="caret"></span></button>
            <ul class="dropdown-menu">
              <li><%= link_to_unless_current "English", locale: "en" %></li>
              <li><%= link_to_unless_current "Русский", locale: "ru" %></li>
            </ul>
        </div>
  </div>
</nav>